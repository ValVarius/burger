<h1>BURGERS</h1>

<h2>EATEN BURGERS!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are devour!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a different Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devour?</label><br>
    <input type="radio" name="devoured" value="1" checked> ALREADY EATEN!<br>
    <input type="radio" name="devoured" value="0"> FRESH BURGER
  </div>

  <button type="submit">Add burger</button>
</form>
