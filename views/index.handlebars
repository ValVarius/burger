<h1>BURGERS</h1>

<h2>ON THE TABLE!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>BURGERS KITCHEN!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a different Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Where are we taking the burger?</label><br>
    <input type="radio" name="devoured" value="1" checked>Bring Recepie to The kitchen!<br>
    <input type="radio" name="devoured" value="0">Bring to the Table!
  </div>

  <button type="submit">Add burger</button>
</form>
